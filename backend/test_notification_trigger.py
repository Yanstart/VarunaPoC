# Fichier temporaire pour déclencher un échec de lint (sera supprimé)
# Ce fichier contient du code volontairement mal formaté
import   os;import sys
x=1;y=2;z=3
def bad_function(   a,b,c   ):
    return a+b+c
