/**
 * Services Module Exports
 *
 * Centralized exports for service classes.
 *
 * @module services
 *
 * @example
 * import { apiService } from './services';
 *
 * const slides = await apiService.fetchSlides();
 */

export { apiService, ApiService, ApiError } from './ApiService.js';
